# Hone — Rename Checklist (Patrick's Manual Steps)

All code, files, and docs have been updated automatically. The items below require your personal login — they cannot be done by Claude.

---

## 1. GitHub — Rename the repository

The repo is currently `patrickpatches/simmer-fresh`. It needs to become `patrickpatches/hone`.

1. Go to: https://github.com/patrickpatches/simmer-fresh/settings
2. Under **General → Repository name**, change `simmer-fresh` → `hone`
3. Click **Rename**

GitHub automatically redirects the old URL, so any saved links won't break immediately. But you should update your local git remote straight after:

```bash
cd "/path/to/Cooking App"
git remote set-url origin https://github.com/patrickpatches/hone.git
```

---

## 2. GitHub Pages — Update deployment target

After renaming the repo, GitHub Pages will be at `patrickpatches.github.io/hone` (the old URL will redirect for a while but don't rely on it).

If you have a custom domain configured in Pages settings, no change needed — the domain stays the same.

If you share the Pages link anywhere (notes, messages, test devices), update it to the new URL.

---

## 3. GitHub PAT (Personal Access Token) — Update if needed

Your current PAT was scoped to `patrickpatches/simmer-fresh`. After the repo rename, the PAT continues to work because GitHub tracks it by repo ID, not name. No action needed unless the token expires (~2026-07-21).

---

## 4. Expo / EAS — Update the project slug

The slug in `app.json` is now `hone`. The EAS project is linked by the `projectId` (`9a50cdfe-5a41-447f-a46a-eddf3e383762`), so the build pipeline won't break. But the project will still show as "simmer-fresh" on the Expo dashboard until you update it.

1. Go to: https://expo.dev/accounts/patrick.nasr/projects
2. Find the Simmer Fresh project
3. Rename it to **Hone** in project settings

---

## 5. Google Play Console — Update the app listing name

The Play Store listing name is separate from the app package ID. Change it here:

1. Go to: https://play.google.com/console
2. Open your app → **Store presence → Main store listing**
3. Change the **App name** field to: `Hone - Recipes & Meal Planning`
4. Save and submit for review if required

Note: The Android package ID is changing from `com.patricknasr.simmerfresh` → `com.patricknasr.hone`. Since the app has not yet been published publicly, this is safe. The new package ID will go into effect with your first EAS build.

---

## 6. Run expo prebuild before your first EAS build

The Android folder was generated by Expo and still has a stale `simmerfresh` folder inside it. Running prebuild will regenerate the Android native folder cleanly with the new package name:

```bash
cd mobile
npx expo prebuild --clean
```

This wipes and regenerates `android/` from scratch using the new `app.json` values. Run this **before** triggering an EAS build.

---

## 7. ATO spreadsheet — Rename note

The file has been copied to `Hone_Development_Log_FY2025-26.xlsx`. The old `Simmer_Fresh_Development_Log_FY2025-26.xlsx` still exists in the folder — you can delete it once you've confirmed the new file is intact. If you have the spreadsheet open in Excel, close and reopen the new file.

---

## Summary — what's already done (no action needed)

| Location | Status |
|---|---|
| `mobile/app.json` — name, slug, scheme, bundle IDs | ✅ Done |
| `mobile/package.json` — name | ✅ Done |
| `mobile/android/build.gradle` — namespace, applicationId | ✅ Done |
| `mobile/android/AndroidManifest.xml` — scheme | ✅ Done |
| `mobile/android/settings.gradle` — project name | ✅ Done |
| `mobile/android/strings.xml` — app_name | ✅ Done |
| `mobile/android/.../hone/MainActivity.kt` — package | ✅ Done |
| `mobile/android/.../hone/MainApplication.kt` — package | ✅ Done |
| `mobile/src/data/seed-recipes.ts` — Hone Kitchen credits | ✅ Done |
| `hone.html` — title, header, shopping list, credits | ✅ Done |
| `index.html` — synced with hone.html | ✅ Done |
| `CLAUDE.md` — all references | ✅ Done |
| `docs/roadmap.md` — all references | ✅ Done |
| `Hone_Development_Log_FY2025-26.xlsx` — file renamed | ✅ Done |
| Memory files — all updated | ✅ Done |
| TypeScript build — zero errors | ✅ Verified |
| GitHub repo rename | ⏳ Patrick |
| Expo dashboard project rename | ⏳ Patrick |
| Play Console listing name | ⏳ Patrick |
| `expo prebuild --clean` before EAS build | ⏳ Patrick |
