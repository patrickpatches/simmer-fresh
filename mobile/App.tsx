// Legacy entry point. The real entry is `expo-router/entry`
// (set in package.json `main`). This file is kept only because the
// workspace sandbox can't delete files. It is never executed.
export default function UnusedLegacyEntry() {
  return null;
}
